# Copy to .env and edit if your database isn't the local docker-compose one.
# These values already match docker-compose.yml, so local dev works without a .env.
DATABASE_URL=postgresql+psycopg://study_notes:study_notes@localhost:5432/study_notes
